{
	"Print to console": {
		"scope": "java",
		"prefix": "bssudokuValitate",
		"body": [
			" private static boolean check(int[][] board) {for (int i = 0; i < board.length; i++) { int sumRow = 0; int sumColumn = 0; for (int j = 0; j < board.length; j++) { sumColumn += board[i][j];sumRow += board[j][i];}if (sumRow != 45 || sumColumn != 45) {return false; } } return true;}"
		],
		"description": "Log output to console"
	}
}