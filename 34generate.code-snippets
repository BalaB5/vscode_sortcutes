{
	"Print to console": {
		"scope": "java",
		"prefix": "bs34generate",
		"body": [
			"static void generate(int n) { String[] arr = new String[n + 1];arr[0] = \"\"; int size = 1, m = 1;  while (size <= n) {for (int i = 0; i < m && (size + i) <= n; i++)arr[size + i] = \"3\" + arr[size - m + i];for (int i = 0; i < m && (size + m + i) <= n; i++) {arr[size + m + i] = \"4\" + arr[size - m + i];}m *= 2;size = size + m;}for (int i = 1; i < arr.length; i++) {System.out.println(arr[i]);} }"
		],
		"description": "Log output to console"
	}
}